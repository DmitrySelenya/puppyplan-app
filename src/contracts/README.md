# Contracts

Shared PuppyPlan semantic contracts live here.

`navigation.ts` is the only scaffold-time contract. Product payload schemas, business rules, generated DB type re-exports, analytics events, and Edge Function request/response schemas are deferred to their owning Linear issues and plans.
