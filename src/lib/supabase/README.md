# Supabase Boundary

Raw Supabase clients and wrappers will live here. Feature UI and route files must not import `@supabase/supabase-js` directly.

PUP-3 owns backend contracts, migrations, generated DB types, RLS tests, and Edge Function setup.
