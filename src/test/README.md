# Tests

Scaffold checks currently run through `scripts/checks`.

The Jest/Vitest or React Native Testing Library harness is deferred to PUP-4. Do not land behavior-bearing feature code with only the scaffold checks as coverage.
